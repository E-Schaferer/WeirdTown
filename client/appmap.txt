-index
  -Router (no file)
    -Auth0ProviderWithHistory (no file)
      -App (state)
        -CurrentUser
        -AuthBar
        -Mapzone (state)
        -AbsentStory
          -StoryForm
        -PresentStory (state)
          -SubStory
            -SubStoryList (state)
              -SubStoryRender
            -SubStoryForm